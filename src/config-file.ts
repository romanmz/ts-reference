/*

compilerOptions
--------------------------------------------------

rootDir             // input folder
outDir              // output folder
sourceMap           // if set to 'true', generates source maps for each generated .js file

target                          // ES version to transpile to
strictNullChecks                // if true, disables the default behavior of allowing 'null' and 'undefined' to be assigned to any variable regardless of its type
strictFunctionTypes             // if true, enables strict type checking in function arguments
strictBindCallApply             // enable strict checking of 'bind', 'call', and 'apply' methods on functions
strictPropertyInitialization    // enable strict checks to prevent class props that don't have a default value and are not initialized inside the constructor method

*/
